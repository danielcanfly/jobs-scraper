# ─────────────────────────────────────────────────────────────
# 依賴套件 (Python 3.11+)
# 安裝: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# 1. LinkedIn Guest API (無 cookie 抓取)
#    curl_cffi: 偽裝瀏覽器 TLS fingerprint, 避免被 LinkedIn ban
curl-cffi>=0.5.10

# 2. LinkedIn HTML parse
scrapling>=0.4.14,<0.5

# 3. Google Sheets 寫入
gspread>=6.0.0
google-auth>=2.23.0

# 4. 讀 .env 自動載入環境變數
python-dotenv>=1.0.0

# 5. MCP server (給 Codex / 其他 MCP 工具用)
#    v1.1.x provides crawl_jobs / initialize_job_tracker / sync_jobs_to_sheet / audit_sheet / get_stats
#    升 MCP v2 stable line (FastMCP 已 rename 為 MCPServer)
mcp>=2.0,<3
